<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= title %></title>
    <link
      rel="stylesheet"
      href="	https://cdn.jsdelivr.net/npm/bootstrap@5.3.1/dist/css/bootstrap.min.css"
    />
  </head>
  <body>
    <div class="container">
      <h1><%= title %></h1>
      <form action="/" method="post">
        <div class="form-group mb-2">
          <input type="text" class="form-control mb-2" name="title" />
          <button class="btn btn-primary" type="Submit">Create!</button>
        </div>
      </form>

      <% if(notes.length) { %>
      <div class="card">
        <ul class="list-group">
          <% for(let i = 0; i < notes.length; i++) { %>
          <li
            class="list-group-item d-flex flex-nowrap justify-content-between align-item-center"
          >
            <p><%= notes[i].title %></p>
            <div>
              <button
                class="btn btn-primary"
                data-type="edit"
                data-id="<%= notes[i].id %>"
              >
                Edit task
              </button>
              <button
                class="btn btn-danger"
                data-type="remove"
                data-id="<%= notes[i].id %>"
              >
                &times;
              </button>
            </div>
          </li>

          <% } %>
        </ul>
      </div>
      <% } else { %>
      <p>No Task!</p>
      <% } %>
    </div>

    <script src="/app.js"></script>
  </body>
</html>
